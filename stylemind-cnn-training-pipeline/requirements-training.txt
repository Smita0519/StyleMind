# Requirements for the TRAINING-ONLY package (Steps 7-10).
# This machine only builds tf.data pipelines, builds/trains the Keras
# model, and evaluates it - the heavy CV preprocessing (YOLO-seg, CLIP)
# already happened elsewhere and is not this machine's job.
#
# No torch, ultralytics, huggingface_hub, or open_clip_torch needed here.

tensorflow>=2.15,<3.0
numpy>=1.26,<2.0
Pillow>=10.0
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
